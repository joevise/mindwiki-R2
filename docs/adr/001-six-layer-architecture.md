# ADR-001: 六层架构与层间 trait 隔离

日期：2026-08-18 · 状态：已接受

## 决策
六层：L0 数据 / L1 安全 / L2 知识库 / L3 运行时 / L4 服务 / L5 接口。
层间只许向下依赖；跨层通过 trait（如 SecretStore）依赖倒置。

## 动机
- 知识库模块升级不动加密层（反之亦然）
- 安全边界清晰可审计（密钥只在 L1）
- 每层独立 crate、独立测试

## 后果
- mw-* 各 crate 独立演进
- 上游（r2-agent / besureAI）经 git 依赖引入，tag 锁版本
